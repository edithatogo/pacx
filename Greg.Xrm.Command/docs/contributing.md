# Contributing

Use the repository-level `CONTRIBUTING.md` as the source of truth for local setup, commit hygiene, and pull request expectations.

Before opening a pull request:

- Run the relevant build and test commands.
- Keep Conventional Commit messages.
- Update documentation for user-visible command behavior.
- Add or update an ADR when changing a stable architecture boundary.
